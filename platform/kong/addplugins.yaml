add-plugins:
  - selectors:
      - $..services[*]
    overwrite: yes
    plugins:
      - name: response-transformer
        config:
          add:
            headers:
              - "X-Engie-Security: passed"